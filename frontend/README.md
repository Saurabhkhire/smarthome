# Aria React

## Map

**Leaflet + OpenStreetMap** only — no Google API key, no billing.

```bash
cd frontend
npm install
npm run dev
```

Backend on **:8000** (see `vite.config.js` proxy).

Respect [OSM tile usage](https://operations.osmfoundation.org/policies/tiles/) for production traffic.
